<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="jpa_training">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <properties>
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver" />
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/jpa_training" />
            <property name="javax.persistence.jdbc.user" value="app_user" />
            <property name="javax.persistence.jdbc.password" value="app_pass" />
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect" />

            <!-- Autodetect entities through @Entity annotations and *.hbm.xml files -->
            <property name="hibernate.archive.autodetection" value="class,hbm" />

            <!-- Use logging through logger instead -->
            <!--<property name="hibernate.show_sql" value="true" />-->

            <!-- Validate the DB schema using the Hibernate mappings -->
            <property name="hibernate.hbm2ddl.auto" value="validate" />
        </properties>
    </persistence-unit>

</persistence>
